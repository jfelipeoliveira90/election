server:
  context-path: "/api"
spring:
  datasource:
    url: "jdbc:mariadb://localhost:3306/election"
    username: "root"
    password: "root"
    driverclassName: "org.mariadb.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: "create-drop"